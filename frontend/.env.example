ADMIN_USERNAME=admin
# Generate with: python3 -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt()).decode())"
ADMIN_PASSWORD_HASH=
# Must match ADMIN_API_KEY in the backend's .env (project root).
ADMIN_API_KEY=
